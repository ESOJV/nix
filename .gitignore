# build output symlinks (never commit these — they're rebuilt locally)
result
result-*

# tmux runtime plugins (installed by TPM, not config)
dotfiles/tmux/plugins/

# stray Claude Code artifacts that land in config dirs
.claude/
